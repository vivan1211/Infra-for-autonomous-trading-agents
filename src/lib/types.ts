/**
 * Shared TypeScript types matching both mock data and API responses.
 * Re-exports API types for convenience.
 */

export type {
  Agent,
  AgentStatus,
  Credential,
  CredentialTestResult,
  Market,
  MarketList,
  Category,
  Trade,
  TradeList,
  Portfolio,
  PortfolioSnapshot,
  KalshiBalance,
  RulesConfig,
  LogEntry,
} from './api';
